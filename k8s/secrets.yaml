apiVersion: v1
kind: Secret
metadata:
  name: algo-trading-secrets
  namespace: algo-trading
  labels:
    app.kubernetes.io/name: algo-trading-lab
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Exchange API keys (replace with actual values)
  BINANCE_API_KEY: "your-binance-api-key"
  BINANCE_API_SECRET: "your-binance-api-secret"
  
  # API authentication
  API_KEY: "your-api-key-here"
  
  # OpenAI for AI features (optional)
  OPENAI_API_KEY: "your-openai-api-key"
  
  # Database credentials (if using external DB)
  DB_PASSWORD: "your-db-password"
---
# Note: In production, use Kubernetes External Secrets or Sealed Secrets
# Example with External Secrets Operator:
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: algo-trading-external-secrets
#   namespace: algo-trading
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     kind: ClusterSecretStore
#     name: vault-backend
#   target:
#     name: algo-trading-secrets
#   data:
#     - secretKey: BINANCE_API_KEY
#       remoteRef:
#         key: trading/binance
#         property: api_key
